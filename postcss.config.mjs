// PostCSS config: wires Tailwind v4 as PostCSS plugin (build pipeline)
const config = {
  // plugins map
  plugins: {
    // tailwind postcss entry
    "@tailwindcss/postcss": {},
  },
};

// default export for tooling
export default config;
