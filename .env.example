# copy → .env.local (gitignored) for local secrets
# never commit real API keys

# public to browser (prefix required for client exposure)
NEXT_PUBLIC_APP_NAME=anatomy-demo

# server-only example (no NEXT_PUBLIC_ prefix)
# DATABASE_URL=postgresql://user:pass@localhost:5432/db
